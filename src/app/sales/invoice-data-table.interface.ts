export interface InvoiceDataTable {
          customername,
          invoiceno,
          invoicedate,
          duedate,
          subtotal,
          total,
          paidamount,
          action
}
