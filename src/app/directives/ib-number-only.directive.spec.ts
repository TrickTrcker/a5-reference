import { IbNumberOnlyDirective } from './ib-number-only.directive';

describe('IbNumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new IbNumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
